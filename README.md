# cryoSPHERE clustering
One of the advantages of cryoSPHERE is that it outputs structures instead of volumes. This means that the analysis can be performed in structure space instead of latent space.

This repository provides tools for clustering the structure that cryoSPHERE outputs.
